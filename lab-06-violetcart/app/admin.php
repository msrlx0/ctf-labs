<?php
require_once __DIR__ . '/includes/layout.php';
page_header('Admin');
?>
<section class="panel">
  <p class="eyebrow">Admin</p>
  <h1>Admin console parked</h1>
  <p class="muted">This legacy console is retained for QA screenshots during the checkout migration. Seller review, reservation sync, and settlement policy are handled by separate operations desks.</p>
  <pre class="code">QA placeholder only: FLAG{qa_placeholder_ignore_me}
This marker is fake and is not a challenge flag.</pre>
  <p class="fine-print">Failed login attempts, common SQL probes, and old admin URLs are logged for training realism but are not the purchase path.</p>
</section>
<?php page_footer(); ?>
