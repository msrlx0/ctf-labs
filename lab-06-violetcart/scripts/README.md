# Scripts

Maintainer validation scripts are intentionally not included in the public release.

The private development branch previously used local validation automation to check the full intended path and secondary vulnerability behavior. Those scripts reveal too much challenge methodology for a student-facing release.

For public validation, use the smoke checks in `../VALIDATION.md`.
