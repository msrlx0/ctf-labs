<%- include('partials/header', { title: 'Webhook' }) %>

<section class="panel">
  <p class="eyebrow">Ferramenta de integracao</p>
  <h1>Webhook Tester</h1>
  <p class="muted">Adaptadores internos respondem apenas de dentro do vault. Healthchecks locais costumam denunciar serviços esquecidos.</p>
  <form class="form-stack" action="/tools/webhook" method="post">
    <label>
      URL alvo
      <input name="url" value="<%= targetUrl %>" placeholder="https://example.com" required>
    </label>
    <button type="submit">Enviar teste</button>
  </form>
  <% if (error) { %>
    <div class="alert"><%= error %></div>
  <% } %>
  <% if (response) { %>
    <div class="response-head">
      <h2>Resposta</h2>
      <span class="status-pill">server-side probe</span>
    </div>
    <pre class="trace"><code><%= response %></code></pre>
  <% } %>
</section>

<%- include('partials/footer') %>
